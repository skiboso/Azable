[package]
name = "water-technician"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
publish = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
